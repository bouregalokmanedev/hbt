import {
    ArrowLeft,
    Award,
    BookOpen,
    CheckCircle2,
    ClipboardCheck,
    History,
} from "lucide-react";
import {
    Link,
    useParams,
} from "react-router-dom";
import {
    useQuery,
} from "@tanstack/react-query";

import {
    getInstructorStudent,
} from "../api/instructorApi";

export function InstructorStudentProfilePage() {
    const { studentId } = useParams();
    const profile = useQuery({ queryKey: ["instructor", "student", studentId], queryFn: () => getInstructorStudent(Number(studentId)), enabled: Boolean(studentId) });
    if (profile.isLoading) return <div className="space-y-5"><div className="h-32 animate-pulse rounded-2xl bg-black/5" /><div className="h-72 animate-pulse rounded-2xl bg-black/5" /></div>;
    if (profile.isError || !profile.data) return <div className="rounded-2xl border border-red-200 bg-red-50 p-6"><h1 className="font-semibold text-red-900">Learner unavailable</h1><p className="mt-2 text-sm text-red-700">This learner is not enrolled in one of your courses.</p></div>;
    const data = profile.data;
    return <div className="mx-auto max-w-5xl space-y-6"><header><Link to="/instructor/students" className="inline-flex items-center gap-1.5 text-xs font-semibold text-[#3A3A3A]/50 hover:text-[#F47822]"><ArrowLeft className="h-3.5 w-3.5" /> Students</Link><div className="mt-5 rounded-2xl bg-[#3A3A3A] p-6 text-white"><p className="text-[10px] font-bold uppercase tracking-[.16em] text-[#F9A16C]">Learner profile</p><h1 className="mt-3 text-2xl font-semibold">{data.student.name}</h1><p className="mt-1 text-sm text-white/60">{data.student.email}</p></div></header><section className="rounded-2xl border border-[#3A3A3A]/8 bg-white p-5 shadow-[0_8px_30px_rgba(58,58,58,.04)]"><SectionHeading icon={BookOpen} title="Course progress" subtitle="Enrollment and progress across your courses." /><div className="mt-5 space-y-3">{data.courses.map((item) => <article key={item.course.id} className="rounded-xl border border-[#3A3A3A]/7 bg-[#FCFCFC] p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="font-semibold text-[#3A3A3A]">{item.course.title}</h2><p className="mt-1 text-xs text-[#3A3A3A]/45">{item.progress.completed_lessons} lessons completed · {Math.round(item.progress.time_spent / 60)} min learned</p></div><span className="rounded-full bg-[#F47822]/10 px-2.5 py-1 text-xs font-bold text-[#F47822]">{item.progress.percentage}%</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-[#3A3A3A]/7"><div className="h-full rounded-full bg-[#F47822]" style={{ width: `${item.progress.percentage}%` }} /></div></article>)}</div></section><div className="grid gap-6 lg:grid-cols-2"><AttemptList title="Quiz performance" icon={ClipboardCheck} empty="No completed quiz attempts yet." items={data.quiz_attempts.map((item) => ({ id: item.id, title: item.quiz_title, course: item.course_title, score: `${item.score}%`, passed: item.passed }))} /><AttemptList title="Assessment results" icon={Award} empty="No submitted assessments yet." items={data.assessment_attempts.map((item) => ({ id: item.id, title: item.assessment_title, course: item.course_title, score: item.score === null ? "Pending" : `${item.score}%`, passed: Boolean(item.passed) }))} /></div><section className="rounded-2xl border border-[#3A3A3A]/8 bg-white p-5 shadow-[0_8px_30px_rgba(58,58,58,.04)]"><SectionHeading icon={Award} title="Certificate history" subtitle="Certificates issued for the learner in your courses." />{data.certificates.length ? <div className="mt-5 divide-y divide-[#3A3A3A]/7">{data.certificates.map((certificate) => <div key={certificate.id} className="flex items-center justify-between gap-3 py-3 first:pt-0 last:pb-0"><div className="min-w-0"><p className="truncate text-sm font-semibold text-[#3A3A3A]">{certificate.course_title}</p><p className="mt-1 text-xs text-[#3A3A3A]/45">{certificate.certificate_number}</p></div><p className="text-[11px] text-[#3A3A3A]/40">{formatDate(certificate.issued_at)}</p></div>)}</div> : <p className="mt-5 rounded-xl bg-[#FCFCFC] px-4 py-6 text-center text-xs text-[#3A3A3A]/45">No certificates issued in your courses.</p>}</section><section className="rounded-2xl border border-[#3A3A3A]/8 bg-white p-5 shadow-[0_8px_30px_rgba(58,58,58,.04)]"><SectionHeading icon={History} title="Recent learning activity" subtitle="Recent learning activity in your courses." />{data.activity.length ? <div className="mt-5 divide-y divide-[#3A3A3A]/7">{data.activity.slice(0, 8).map((item, index) => <div key={`${item.type}-${item.occurred_at}-${index}`} className="flex items-center gap-3 py-3 first:pt-0 last:pb-0"><div className="h-2 w-2 rounded-full bg-[#F47822]" /><div className="min-w-0 flex-1"><p className="text-sm font-semibold text-[#3A3A3A]">{item.title}</p><p className="mt-1 truncate text-xs text-[#3A3A3A]/45">{item.detail}</p></div><p className="text-[11px] text-[#3A3A3A]/40">{formatDate(item.occurred_at)}</p></div>)}</div> : <p className="mt-5 rounded-xl bg-[#FCFCFC] px-4 py-6 text-center text-xs text-[#3A3A3A]/45">No recent learning activity.</p>}</section></div>;
}

function SectionHeading({ icon: Icon, title, subtitle }: { icon: typeof BookOpen; title: string; subtitle: string }) { return <div className="flex items-start gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-[#F47822]/10 text-[#F47822]"><Icon className="h-4 w-4" /></div><div><h2 className="font-semibold text-[#3A3A3A]">{title}</h2><p className="mt-1 text-xs text-[#3A3A3A]/45">{subtitle}</p></div></div>; }
function AttemptList({ title, icon, empty, items }: { title: string; icon: typeof BookOpen; empty: string; items: Array<{ id: string; title: string; course: string; score: string; passed: boolean }> }) { return <section className="rounded-2xl border border-[#3A3A3A]/8 bg-white p-5 shadow-[0_8px_30px_rgba(58,58,58,.04)]"><SectionHeading icon={icon} title={title} subtitle="Latest submitted results." />{items.length ? <div className="mt-5 divide-y divide-[#3A3A3A]/7">{items.map((item) => <div key={item.id} className="flex items-center gap-3 py-3 first:pt-0 last:pb-0"><CheckCircle2 className={`h-4 w-4 shrink-0 ${item.passed ? "text-emerald-500" : "text-[#3A3A3A]/25"}`} /><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-[#3A3A3A]">{item.title}</p><p className="mt-1 truncate text-xs text-[#3A3A3A]/45">{item.course}</p></div><span className="text-sm font-bold text-[#3A3A3A]">{item.score}</span></div>)}</div> : <p className="mt-5 rounded-xl bg-[#FCFCFC] px-4 py-6 text-center text-xs leading-5 text-[#3A3A3A]/45">{empty}</p>}</section>; }

function formatDate(value: string | null): string { return value ? new Intl.DateTimeFormat(undefined, { month: "short", day: "numeric" }).format(new Date(value)) : "Recently"; }
